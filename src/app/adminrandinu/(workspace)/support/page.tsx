import { CheckCircle2, Clock3, MessageCircleMore } from "lucide-react";
import { updateSupportStatusAction } from "@/app/actions/admin";
import { PageHeading } from "@/components/page-heading";
import { StatusBadge } from "@/components/status-badge";
import { getAdminData, getAdminSupportRequests } from "@/lib/data";
import { displayPhone } from "@/lib/auth";
export default async function AdminSupportPage(){const[data,requests]=await Promise.all([getAdminData(),getAdminSupportRequests()]);return <div><PageHeading eyebrow="STUDENT ASSISTANCE" title="Support Requests" description="Review account, payment, access, result and technical issues submitted from the LMS."/><section className="support-admin-list">{requests.map((request)=>{const student=data.students.find((item)=>item.id===request.studentId);return <article key={request.id}><header><span className="support-request-icon"><MessageCircleMore/></span><div><div className="student-title-row"><h3>{request.subject}</h3><StatusBadge tone={request.status==="resolved"?"success":request.status==="in_progress"?"brand":"warning"}>{request.status.replace("_"," ")}</StatusBadge></div><p>{student?.fullName??request.contactNumber??"Unmatched request"}{student&&` · ${displayPhone(student.phone)}`}</p><small><Clock3 size={14}/>{new Date(request.createdAt).toLocaleString("en-LK")}</small></div></header><div className="support-request-message">{request.message}</div><form action={updateSupportStatusAction} className="support-resolution-form"><input type="hidden" name="requestId" value={request.id}/><select name="status" defaultValue={request.status}><option value="open">Open</option><option value="in_progress">In progress</option><option value="resolved">Resolved</option><option value="closed">Closed</option></select><input name="adminNotes" defaultValue={request.adminNotes||""} placeholder="Internal admin note"/><button className="button button-dark button-small"><CheckCircle2 size={15}/>Update</button></form></article>})}</section></div>}
