import { Database, KeyRound, Settings, ShieldCheck } from "lucide-react";
import { PageHeading } from "@/components/page-heading";
import { isSupabaseConfigured } from "@/lib/env";
export default function AdminSettingsPage(){const configured=isSupabaseConfigured();return <div><PageHeading eyebrow="PLATFORM CONFIGURATION" title="Settings" description="Review production readiness, database connectivity and security-sensitive configuration."/><section className="settings-grid"><article className="admin-card"><span className="settings-icon"><Database/></span><h2>Supabase connection</h2><p>Authentication, PostgreSQL, Row Level Security and private resource storage.</p><div className={`settings-status ${configured?"ready":"warning"}`}><ShieldCheck size={17}/>{configured?"Environment variables detected":"Running with demonstration data"}</div></article><article className="admin-card"><span className="settings-icon"><KeyRound/></span><h2>Student login method</h2><p>Students sign in using their mobile number and password. A private email alias is generated internally for Supabase Auth.</p><div className="settings-status ready"><ShieldCheck size={17}/>No public student email required</div></article><article className="admin-card"><span className="settings-icon"><Settings/></span><h2>Access model</h2><p>Pending users can use free content. Paid access requires verification, enrolment and monthly payment—or an explicit admin override.</p><div className="settings-status ready"><ShieldCheck size={17}/>Designed for cross-program sharing</div></article></section><section className="admin-card deployment-checklist"><span className="section-kicker">PRODUCTION CHECKLIST</span><h2>Before inviting students</h2><div><label><input type="checkbox" checked readOnly/>Run <code>supabase/schema.sql</code> in a new Supabase project</label><label><input type="checkbox" checked={configured} readOnly/>Add Supabase environment variables in Vercel</label><label><input type="checkbox"/>Create the first admin user using <code>supabase/create-admin.sql</code></label><label><input type="checkbox"/>Publish bank details and social links</label><label><input type="checkbox"/>Test registration, verification, payment and assessment flows</label></div></section></div>}
