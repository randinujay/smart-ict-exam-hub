"use client";
import { useActionState } from "react";
import { LoaderCircle, Send } from "lucide-react";
import { initialActionState } from "@/lib/action-state";
import { createSupportRequestAction } from "@/app/actions/student";
export function SupportForm(){const[state,action,pending]=useActionState(createSupportRequestAction,initialActionState);return <form action={action} className="support-form"><label className="field"><span>What do you need help with?</span><select name="requestType" required defaultValue=""><option value="" disabled>Select a category</option><option value="account">Account verification</option><option value="access">Module or resource access</option><option value="payment">Payment status</option><option value="result">Assessment or result</option><option value="profile_correction">Profile correction</option><option value="technical">Technical issue</option><option value="other">Other</option></select></label><label className="field"><span>Subject</span><input name="subject" required placeholder="Briefly describe the issue"/></label><label className="field"><span>Message</span><textarea name="message" rows={6} required placeholder="Include the relevant month, program or assessment where possible."/></label>{state.message&&<p className={`form-message ${state.ok?"success":"error"}`}>{state.message}</p>}<button className="button button-primary" disabled={pending}>{pending?<LoaderCircle className="spin" size={18}/>:<Send size={17}/>}Submit support request</button></form>}
